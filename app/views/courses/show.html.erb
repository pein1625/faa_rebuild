<%
  meta_title @course.name
  meta_keywords t(".keywords", technique: @course.technique, category: @course.category)
  meta_description t(".description", technique: @course.technique, category: @course.category)
  meta_author Settings.author
%>
<div id="breadcrumb" class="full-breadcrumb">
  <div class="container">
    <div class="header-page row">
      <div class="title-page col-md-6 col-sm-6">
        <h2><%= t ".detail_course" %></h2>
      </div>
      <div class="breadcrumbs col-md-6 col-sm-6">
        <ul>
          <li>
            <%= link_to t(".home"), root_path %>
          </li>
          <li>
            <%= link_to t(".course"), courses_path %>
          </li>
          <li class="active"><span><%= t ".detail_course" %></span></li>
        </ul>
      </div>
    </div>
  </div>
</div>

<div id="main-content" class="full-content">
  <div class="container">
    <div class="row">
      <div id="component" class="col-md-9">
        <div class="banner">
          <%= load_course_image @course, :cover, "image-w880-h337" %>
        </div>
        <div class="detail-class">
          <%= render "course_header", course: @course,
            newest_schedule: @course.newest_schedule %>
          <div class="description">
            <%= markdown @course.content %>
          </div>
        </div>
      </div>

      <%= render "right_sidebar_course" %>
    </div>
  </div>
</div>
